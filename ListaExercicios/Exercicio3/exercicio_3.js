let num1 = 20, num2 = 40;

console.log (`A variável ${num1} é menor que á variável ${num2}`);

// Ou no método math.x()

let maior = Math.max(200, 30);
let menor = Math.min(200, 30);

console.log(`A variável maior é ${maior}, enquanto a menor é ${menor}`);
